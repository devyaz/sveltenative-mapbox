import {Observable,EventData} from "@nativescript/core/data/observable"
import { ObservableArray} from "@nativescript/core/data/observable-array";
var utils = require("@nativescript/core/utils/utils");

export const model = new Observable()
/*
model.background=function(){
    services.setupAlarm(utils.ad.getApplicationContext());
    console.log(" trying to set alarm!!");
}*/

 

//exports.defaults  = model;